<h1>All Artists</h1>

<% @artists.each do |artist| %>
  <h2><%= link_to artist.name, artist_path(artist) %></h2>
  <h3><%= pluralize(artist.song_count, 'Song') %></h2>
  <% artist.songs.each do |song| %>
    <h4><%= link_to song.title, song_path(song) %></h4>
  <% end %>
<% end %>
